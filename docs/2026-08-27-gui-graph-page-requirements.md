# GUI 图谱页需求（管理下新增"图谱"栏目）

日期：2026-08-27
状态：已实现（feat/gui-graph-page，2026-08-27 验收通过）
前置文档：
- `docs/prototypes/prototype-wiki-graph.html`（**渲染与交互规范源**：本需求锁定的技术路线，
  纯手写 SVG 力导向、单文件零依赖；拖拽选中蓝框、悬停邻居淡化等已修复行为以该文件为准）
- `docs/2026-08-27-wiki-graph-layered-design.md`（同一路线的规模扩容预案：
  骨架常显 + 类目下钻 + DOM 懒挂载，R3 的解构方案即来自此文档）
- 注：仓库内另有 `prototype-wiki-graph-v2.html` / sigma 版等探索性原型，经评审**不作为**
  本次需求的实现基线，仅作备选参考。

## 0. 一句话定位

把 `prototype-wiki-graph.html` 的图谱能力从原型 HTML 搬进 Web GUI，成为左侧导航的一级栏目：
**按项目查看知识条目关系图，图上能定位、能下钻，条目能一键跳回管理页。**

## 1. 需求条目

### R1 左侧导航新增"图谱"栏目，位于"管理"下方

- `web/app.js:342` 的 `MENUS` 在 `manage` 之后插入 `{ key:"graph" }`，ICON/i18n 文案沿用现有模式
  （`t()` 词条中英双语，中文"图谱"）。
- 菜单路由沿用 `location.hash` 改写机制；token 注入/sessionStorage 兜底链路不变
  （`web/index.html` 既有约定），图谱页所有数据请求走统一 `api()` helper。
- 无项目时的空态与管理页一致：落引导提示，不渲染空图。

### R2 页面内可选择不同项目

- 页面顶栏左侧放项目下拉选择器（样式对齐"其他"页的项目下拉）。
- 数据源 = `GET /api/projects`（复用管理页同一缓存口径，按 last_update 降序，默认选中最近更新项目）。
- 切换项目 = 重建整张图（数据、布局、选中态全部重置）；选择不做跨页同步，
  与管理页的项目选择互不干扰。

### R3 数据展现当前项目的全部条目；条目量大时按"骨架常显 + 类目下钻"解构

数据范围：所选项目的**全部条目**（pitfall/note/rule/reference 四型全收），
不是只画 wiki 骨架——图谱的价值就在看踩坑条目挂在谁身上。

解构方案（采用 `2026-08-27-wiki-graph-layered-design.md` 为 v1 路线定的预案）：

1. **类目（category）= 条目一级分组**，聚合/着色的最小单位。口径沿用管理页树的类目分组
   （`groupEntries`/`catKeyOf` 同一套），保证图谱与管理页看到的分组一致。
2. **两级节点**：
   - 骨架层（常显）：目录条目（菱形）+ 高连接度条目（`deg >= 阈值` 的大球）+ 它们之间的边；
   - 叶子层（按需）：其余条目，初始不进 DOM、不参与物理模拟（是完全不创建元素，
     不是 v1 图例 `dim` 那种"淡化但元素还在"的做法）。
3. **下钻交互**：图例/类目簇点击 = 把该类目叶子节点创建进 DOM 并做局部收敛；
   再点收起 = 从 DOM 摘除但保留坐标，再次展开无需重算布局；顶栏有"返回总览"。
4. **自动降级阈值**：条目数 ≤400（阈值调试后定）走原型既有全量模式，行为与
   `prototype-wiki-graph.html` 完全一致；超过则自动进入分层模式。
   当前 OpenKnowledge 项目（86 条）在全量档，零感知。
5. **边的口径**（需要后端新契约，见 §2）：
   - struct 边：类目/目录归属关系（目录条目 → 其成员条目）；
   - ref 边：条目正文/tags 中的跨条目引用（现有 wiki 条目互引关系）。

### R4 右侧详情面板的关联条目，双击跳回管理页并定位

- 图谱页单击小球 → 右侧滑出条目详情面板（原型既有行为：标题/类型/tags/摘要/关联条目列表）。
- 面板中的**关联条目行双击** → 切换到"管理"页，打开该条目详情，
  且管理页左侧树**展开到该条目所在类目并滚动定位**到该条目行。
- 实现直接复用管理页现成的 `jumpToEntry(file, project)`（`web/app.js:1952`）——
  终端 search 命中行跳转走的就是它：展开项目手风琴 → 展开类目 → 覆盖懒加载计数 →
  选中条目 → `scrollIntoView` 定位，全套语义已验证。图谱页只需 `state.menu="manage"`
  后调用它。
- 交互坑预警（知识库沉淀）：全量重渲场景下原生 `dblclick` 不可靠，
  双击判定必须走 `dblClick(h, key)` 时间戳检测（`web/app.js:1002`），
  与管理页树节点的四处双击同模式。
- 单击关联条目行的行为：图谱内联动（选中对应节点、图面居中高亮），不跳页——
  单击留在图里探索，双击才去管理页改/看详情。

## 2. 数据契约（后端新增）

新增 `GET /api/graph?project=<名>`，一次返回整张图（当前量级几 KB，无需分页）：

```json
{
  "nodes": [{ "id": "<file 名，与管理页 entrySummaryJSON.file 一致>",
              "title": "...", "type": "pitfall|note|rule|reference",
              "tags": ["..."], "category": "<类目>", "summary": "...",
              "isDir": false, "deg": 3 }],
  "edges": [{ "source": "<file>", "target": "<file>", "kind": "struct|ref" }],
  "categories": ["..."]
}
```

- `id` 用条目 file 名而非标题——这是 R4 能直接喂给 `jumpToEntry` 的关键（它按 file 定位）；
- `deg` 后端算好（边端点计数），前端不再推导；
- ref 边从条目正文/元数据的互引提取，struct 边从目录归属提取；
  提取规则与 `ok wiki` 生成侧的目录口径保持一致；
- 鉴权走现有 token 中间件，无新安全面。

## 3. 验收标准

1. 导航"管理"下方出现"图谱"，进入后默认渲染最近更新项目的全量条目图；
2. 顶栏切换项目，图整体重建且布局稳定（确定性种子，同项目两次进入布局一致）；
3. 悬停节点：相连边橙色高亮 + 非邻居节点淡化（v1 已修行为，搬进 GUI 不回归）；
4. 拖拽节点/平移画布不出现文本选中蓝框（`user-select:none` 修复不回归）；
5. 单击节点出右侧面板；面板关联条目**双击**跳到管理页，条目打开且树滚动定位到该行；
6. 构造 >400 条目的项目自动进入分层模式：首屏只渲染骨架层，类目可展开/收回，
   展开后物理模拟只跑可见集，界面不卡死；
7. 搜索命中分层模式下未展开的叶子条目时，自动展开其所在类目并高亮。

## 4. 非目标

- 图谱页不做条目编辑（编辑在管理页，R4 的跳转就是为此服务）；
- 不做跨项目联合图谱；
- 不更换渲染路线（不引入 Sigma/G6/WebGL，不使用 v2 原型的冻结引擎/折叠球形态——
  规模超出分层预案能力时另行评审，届时再议）；
- 不做 4s 轮询实时刷新（首版进入页面时拉一次；条目变更后手动重进或后续再加失效重拉）。

## 5. 实现排期建议（非需求，仅供参考）

1. 后端 `/api/graph` 契约 + 边提取（独立于前端，可先联调数据）；
2. 图谱页骨架：菜单 + 项目选择器 + `prototype-wiki-graph.html` 的引擎与交互搬进 GUI 页面容器
   （力导向参数、配色、tooltip、图例、面板照抄原型，零依赖）；
3. 详情面板 + R4 跳转接线（`jumpToEntry` 复用）；
4. 分层预案（骨架/下钻/可见集物理/阈值降级）接入真实数据。

## 6. 验收记录（2026-08-27）

环境：隔离 OK_HOME（临时目录，直写 registry.toml + 知识文件播种，全程未跑 ok init/setup）+
`go build` 的工作区 okd（webdir 吃工作区 web/）+ headless Edge 151 CDP 自动化断言（17 项全过）。

实测数据：

- 真实 OpenKnowledge 项目（知识文件拷贝进隔离库，项目名 okreal）：90 节点 / 24 边 /
  4 类目（rule/pitfall/note/reference），目录节点 `架构总览.md`、`演进历程.md`。
- 压测项目 big（直写播种 450 条目）：450 节点 / 410 边，骨架恰好 7 节点
  （2 目录 + 5 枢纽 deg=42）。

| # | 验收标准 | 结果 |
|---|----------|------|
| 1 | 导航"图谱"在"管理"下方，默认渲染最近更新项目全量图 | ✅ 菜单序 [管理, 图谱, …]；默认选中 okreal（kb.db mtime 最新），全量 90 节点 24 边渲染，`layered=false` |
| 2 | 切项目整图重建、布局确定 | ✅ okreal→big→okreal 往返，冻结 rAF 后两次进入 okreal 90 节点坐标逐位一致（mulberry32(42) 重播种） |
| 3 | 悬停：相连边橙色高亮 + 非邻居淡化 | ✅ 悬停 架构总览（deg=18）：18 条边 hot、71（=90-1-18）节点 fade、tooltip 显示；移出全清零 |
| 4 | 拖拽/平移无文本选中蓝框 | ✅ svg `user-select:none`；拖拽 pinned→世界坐标随动→释放全链路，`getSelection()` 为空 |
| 5 | 单击出面板；关联条目双击跳管理页定位 | ✅ 单击 架构总览 面板打开（关联 18 行）；双击"CLI-命令面与项目路由"→ manage 页 `state.sel` 一致、`.leaf.sel` 可见且在视口内 |
| 6 | >400 条目自动分层：骨架常显、类目展开/收回、物理只跑可见集、不卡死 | ✅ big 首屏仅骨架 7 节点 + 10 条骨架间边，rAF 均帧 16.4ms；图例展开 rule → 107 节点入 DOM、均帧 16.6ms、出现"返回总览"；收起回 7、摘 DOM 留坐标 |
| 7 | 搜索命中未展开叶子自动展开类目并高亮 | ✅ 搜 task6searchmark → 自动展开 note 类目（50 可见），目标叶挂载且未 dim、其余 49 可见节点 dim；清空恢复 |

验收 5 的"脏草稿弹确认"子项：评审已查明侧栏导航是无守卫 `exitEdit()`（管理页脏草稿跨页
静默丢弃是既有行为），从图谱页无法构造该场景——记录为**不可达/既有行为，转用户决策**，
本批次不改代码。图谱页自身的 `gJumpToManage` 有 `exitEditGuarded()` 守卫（管理页有脏草稿时
先弹确认，取消则不跳页）。

附带验证：分层模式图例 hint 文案分流（收尾新增 i18n 键 `gLegendHintLayer`）实测显示
"点击图例展开/收起类目"，非分层保持原文案。

### 中心辐射改版（2026-08-28）

原"类目焦点环聚簇 + 冷却冻结"布局改版为中心双锚辐射布局，三笔提交均评审通过：

1. **语义边动态阈值**（`d925a37`，internal/gui/graph.go）：删除固定 `semFloor=0.75`，改按
   目标平均度≈1 分位反推——同一趟 O(n²) 打分循环顺手收集每条目 top-3 候选分值（≤3n 个），
   降序取第 `ceil(0.45×nodeCount×2)` 名（×2 为每候选被两端各提名一次的换算）作阈值，
   钳制 [0.55, 0.75]；nodeCount<10 走 0.55 小库捷径；候选不足取最小值。
   新增 `TestSemThresholdCalibration` 五子用例 + `TestGraphSemanticEdges` 改写，全包测试绿。
2. **中心双锚 + 慢速漂移（撤冻结）**（`98aa302`，web/app.js）：`CLUSTER` 类目焦点环相关
   常量/聚拢力/焦点字段全删，类目只剩配色专用；`架构总览`（左）/`演进历程`（右）两目录
   节点识别为双锚，每帧回中力 `ANCHOR_K=0.01`（无双锚项目自然退化为纯向心+边弹簧）；
   删除"alpha≤0.02 且无拖拽停帧"冻结分支恢复永动 rAF，位移钳 ±12→±1.5、积分速度 ×0.5
   两档制（预跑 12/1、展示 1.5/0.5）；分层展开布点新增黄金角螺旋（r=24×√k）消叠点。
3. **展示期微漂移**（`9041806`，web/app.js）：冻结回退后 idle ~75s 漂移视觉近静止
   （0.0007px/帧），按评审裁决方案加直改位置的微漂移——按节点定相位正弦项
   （DRIFT_AMP=2.0px、x/y 异频 0.031/0.047，逐帧 ≈0.06-0.09px/帧），绕死区/位移钳，
   预跑不施加、拖拽中 pinned 跳过、双锚照常参与。

微漂移实现形态说明：简报钉死的"正弦增量累积进坐标"两轮实测均不达标（密集碰撞笼逐帧
弹回吸收漂移，笼芯节点仅 ~12% 透传，透传率与振幅/频率无关，调参无解），最终落地为
**"帧首撤、帧尾加"可撤漂移层**——帧首把上帧漂移从坐标减掉（物理/碰撞全程只见基座
坐标），帧尾再加新漂移量（用户所见 = 基座 + 漂移层）；碰撞笼吃不到漂移层，透传 100%。

CDP 实测（隔离 okd + headless Edge，90 条目播种库）：

- 双锚识别恰 2 个，屏幕坐标距 stage 中心 124.7/128.3px（<200 达标）；
- 慢速漂移：300 帧最大节点位移 0.189px/帧（∈(0,1.5]）；微漂移 t=100s 采 300 帧
  max=0.114/mean=0.076px/帧（max∈(0,1.5]、mean∈[0.03,0.3] 均达标，t=10s 与 t=100s
  均值稳定 0.080/0.076）；
- 移动中节点点击选中 20/20=100%；
- 回归项全过：双锚拖走 +250px 后 6s 指数回中（25.1→155.7→36.9）；悬停枢纽
  fade/hot/tooltip 及移出清零；搜索 dim/清空恢复；R4 .rel 双击跳管理页；big450 分层
  骨架恰 7→图例展开 rule 107→返回总览回 7、搜索命中未展开叶子自动展开类目；rAF 全程存活。

验证命令（收口复审）：`go test ./internal/gui/ -count=1` 全绿（4.768s）、
`node --check web/app.js` 过、`go build ./...` 过。
